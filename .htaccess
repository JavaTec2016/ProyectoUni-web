RewriteEngine On
RewriteBase /

# Si el archivo o directorio existe, permitir acceso normal
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirigir a index.php para manejar todas las rutas
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
